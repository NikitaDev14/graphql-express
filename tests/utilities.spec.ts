import { fragmentsToString } from '../src/utilities';

describe('Utilities', () => {
  describe('fragmentsToString utility', () => {
    test('fragmentsToString exists', () => {
      expect(fragmentsToString).toBeDefined();
    });

    test('fragmentsToString should return empty string', () => {
      
    });
  });
});
